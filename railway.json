{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "npm install && rm -rf client/envelope-client/build && npm run build"
  },
  "deploy": {
    "startCommand": "node server/index.js"
  }
}
